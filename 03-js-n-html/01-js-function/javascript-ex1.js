function sumar(x, y) {
    console.log("Estoy sumando " + x + " y " + y);
    var resultado = x + y;
    return resultado;
}

var suma = sumar(5,7);

document.write("<h1>La suma es " + suma + "</h1>");